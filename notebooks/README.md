# Notebooks

- `active/`: notebooks that are still useful for exploration or figure work.
- `archived/`: old notebooks, scratch outputs, and editor leftovers kept for reference.

The main Julia model code remains at the repository root in `main.jl` and
`main-rice_mele.jl`.
